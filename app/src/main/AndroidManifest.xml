<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.bummerzaehler"
    android:versionCode="160"
    android:versionName="1.6" android:installLocation="auto">

    <application
        android:allowBackup="true"
        android:icon="@drawable/main_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" 
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TwoPlayerStartActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/two_players"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".CreatePlayerActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/create_player" 
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".TwoPlayerActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/two_players" 
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".FourPlayerActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/four_players" 
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".PlayersActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/players" 
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".FourPlayerStartActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/four_players" 
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".ThreePlayerStartActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/three_players" 
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".ThreePlayerActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/three_players"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".OptionsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/options" 
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".HistoryActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/history" 
            android:launchMode="singleTask">
        </activity>
    </application>

</manifest>
